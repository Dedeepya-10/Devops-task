apiVersion: v1
kind: Service
metadata:
  name: worker
  labels:
    app: worker
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - port: 80
      targetPort: 4100
      name: http
